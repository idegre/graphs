-find dom node willl return the last element that works not the first as i want
-the new double letter algorithm sometimes connects both letter
-make lines algorithm still conects backwads
-spaces too much
-reapeated words get expanded together
-make the makeLine algorithm not sear backwards on the suub level words
-lines not updating correctly
-להקים  
 
ועדת  
 
חקירה  
 
משפטית  
 
והעלוהו  
 
על  
+
 
דרך  
+
 
הפשיעה  
 
יצרה  
 
סרטונים  
 
יהדות  
 
צרפת  
 
דובדבנים  
 
או  
+
 
ריבה  
+
 
דמות  
 
בדיונית  
 
ביקום  
 
דחפים
cross line between to diferent brancehs
repeated words in same level get the same id and the first one from the left or right (ltr or rtl) gets both lines




-basic functionality
	make phrases x
	make diferent levels x
	add subraphs x
		make recursive ( new subraphs apear inside that words div somehow) x
	make each layer smaller than the previous one(could ad a dephth property and interpolate the font size with it)

-make csv file a cron job somewhere
	-add a new first line on each import to get the right keys into it

done:
-obtain JSON (manually)
-install materialUI
-install jquery
-add subwords
-spaces within words not showing up
-add lines
	break up root word into a bucnk of spans to get their positions with jqueryx
	overlay a svg plane (over the spacer?)x

-add search
	-make functional
	-make ltr when nesessary

-make data.json be stored in a firestore
