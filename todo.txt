





-basic functionality
	make phrases x
	make diferent levels x
	add subraphs x
		make recursive ( new subraphs apear inside that words div somehow) x
	make each layer smaller than the previous one(could ad a dephth property and interpolate the font size with it)

-make csv file a cron job somewhere
	-add a new first line on each import to get the right keys into it

done:
-obtain JSON (manually)
-install materialUI
-install jquery
-add subwords
-spaces within words not showing up
-add lines
	break up root word into a bucnk of spans to get their positions with jqueryx
	overlay a svg plane (over the spacer?)x

-add search
	-make functional
	-make ltr when nesessary
